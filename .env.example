NODE_ENV=development
PORT=3000
THROTTLE_TTL=60000
THROTTLE_LIMIT=100
THROTTLE_MINUTE_LIMIT=10
THROTTLE_HOUR_TTL=3600000
THROTTLE_HOUR_LIMIT=100
# Optional — when unset, throttler and cache fall back to in-memory storage.
# Use Render's internal hostname in production (only resolves inside Render's network).
# For local dev, run `docker run -p 6379:6379 redis` and set REDIS_URL=redis://localhost:6379
REDIS_URL=redis://red-d8835en7f7vs73e1592g:6379
CACHE_TTL=300000
FRONTEND_URL=https://www.omarderkaoui.cc/
